import {TAB_ADD, TAB_REMOVE, TAB_SWITCH} from '../actions/tab';

describe('reducers/tabs', () => {
  describe(TAB_ADD, () => {
    //
  });

  describe(TAB_SWITCH, () => {
    //
  });

  describe(TAB_REMOVE, () => {
    //
  });
});
